import React from "react";
import CreateBoard from "../CreateBoard/CreateBoard";

const BoardList = () => {
  return (
    <div>
      <h3>Board List</h3>
      <CreateBoard />
    </div>
  );
};

export default BoardList;
